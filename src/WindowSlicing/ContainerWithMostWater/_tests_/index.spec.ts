import maxArea from '../index';

describe('WindowSlicing/ContainerWithMostWater', () => {
  it(`WindowSlicing/ContainerWithMostWater Pass all tests`, () => {
    expect(maxArea([1, 8, 6, 2, 5, 4, 8, 3, 7])).toEqual(49);
  });
});
